# renumber_all_bacnet_objects
powershell script that renumbers all BACnet objects in a Visio VisualLogic file. It will create 3 files, one .csv file that can be used as a reference for lost values, a backup, and a new copy of the file with RENAMED appended to the end, along with the date and time. This is the file with the changed values.
